node_modules
.next
out
build
*.log
npm-debug.log*

# Environment & secrets — NEVER commit
.env
.env.local
.env*.local
# Anything else sitting beside .env holds the same secrets — .env.bak, .env.old,
# a timestamped copy made before an edit. Ignore the lot; keep only the example.
.env.*
!.env.example

# Bot wallet keystore & database — contain / control real funds
/data
*.keystore.json
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Misc
.DS_Store
next-env.d.ts
*.tsbuildinfo
